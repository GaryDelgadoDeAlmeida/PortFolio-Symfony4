{% extends 'baseAdmin.html.twig' %}

{% block body %}

    {% if message != '' %}
        <div class="{{ message.class }}">
            <span>{{message.message}}</span>
        </div>
    {% endif %}
    
    {{ form(form, {'action': path('adminAbout'), 'method': 'POST'}) }}
    {{ form_end(form, {'action': path('adminAbout'), 'method': 'POST'}) }}
    
{% endblock %}
