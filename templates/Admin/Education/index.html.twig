{% extends 'baseAdmin.html.twig' %}

{% block page_content %}
    <div class="admin-experience">
        <a href="{{ path("adminEducationAdd") }}">
            <button class="btn btn-primary">Add an experience</button>
        </a>

        {# Formation / Experience #}
        <table class="experience-table">
            <thead>
                <tr>
                    <th>Date</th>
                    <th>Type</th>
                    <th>Structure</th>
                    <th></th>
                </tr>
            </thead>
            
            <tbody>
                {% if educations != [] %}
                    {% for education in educations %}
                        {% set status = (education.inProgress == true) ? "En Cours" : education.endDate|date("Y/m") %}
                        <tr>
                            <td>{{ education.startDate|date("Y/m") }} - {{ status }}</td>
                            <td>{{ education.category|capitalize }}</td>
                            <td>{{ education.corporationName }}</td>
                            <td>
                                <a href="{{ path("adminEditEducation", {"id": education.id}) }}">
                                    <img src="{{ asset("/content/images/svg/eye.svg") }}" alt="" />
                                </a>

                                <a href="{{ path("adminDeleteEducation", {"id": education.id}) }}">
                                    <img src="{{ asset("/content/images/svg/close-red.svg") }}" alt="" />
                                </a>
                            </td>
                        </tr>
                    {% endfor %}
                {% else %}
                    <tr>
                        <td colspan="5">
                            <span>Il n'y a aucunes formations ou expériences professionnelles enregistré dans la database.</span>
                        </td>
                    </tr>
                {% endif %}
            </tbody>
        </table>

        {# Pagination #}
        {% if nbrOffsets > 1 %}
            <div class="pagination">
                {% if offset - 1 >= 1 %}
                    <div class="first-page">
                        <a href="{{ path("adminEducation") }}">
                            <img src="{{ asset("/content/images/svg/double-angle-left.svg") }}" alt="double-angle-left" />
                        </a>
                    </div>

                    <div class="previous-page">
                        <a href="{{ path("adminEducationByPage", {"offset": offset - 1}) }}">
                            <img src="{{ asset("/content/images/svg/angle-left.svg") }}" alt="angle-left" />
                        </a>
                    </div>

                    <div class="previous-page">
                        <a href="{{ path("adminEducationByPage", {"offset": offset - 1}) }}">{{ offset - 1 }}</a>
                    </div>
                {% endif %}

                <div class="current-page">
                    <span>{{ offset }}</span>
                </div>
                
                {% if offset + 1 <= nbrOffsets %}
                    <div class="next-page">
                        <a href="{{ path("adminEducationByPage", {"offset": offset + 1}) }}">{{ offset + 1 }}</a>
                    </div>

                    <div class="next-page">
                        <a href="{{ path("adminEducationByPage", {"offset": offset + 1}) }}">
                            <img src="/content/images/svg/angle-right.svg" alt="angle-right" />
                        </a>
                    </div>
                    
                    <div class="last-page">
                        <a href="{{ path("adminEducationByPage", {"offset": nbrOffsets}) }}">
                            <img src="/content/images/svg/double-angle-right.svg" alt="double-angle-right" />
                        </a>
                    </div>
                {% endif %}
            </div>
        {% endif %}
    </div>
{% endblock %}